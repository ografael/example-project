<%= form_with(model: appointment_title) do |form| %>
  <% if appointment_title.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(appointment_title.errors.count, "error") %> prohibited this appointment_title from being saved:</h2>

      <ul>
        <% appointment_title.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :appointment_group_id, style: "display: block" %>
    <%= form.text_field :appointment_group_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
